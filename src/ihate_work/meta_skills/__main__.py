"""Entry point: the interactive skill installer.

Run via `./cli` at the repo root (which ensures the venv exists first).
"""

import sys


def main() -> int:
    print("meta-skills: nothing implemented yet. See AGENTS.md for the design.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
