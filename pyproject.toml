[project]
name = 'meta-skills'
version = '0.1.0'
requires-python = '>=3.13'

[tool.uv]
exclude-newer = "8d"

[tool.ruff]

[tool.ruff.lint]
ignore = [
    'E402',
    'F401', # unused import
    'F541', # f-string is missing placeholders
]
